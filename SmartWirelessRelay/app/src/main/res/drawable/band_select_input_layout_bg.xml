<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:useLevel="false">
    <!--圆角弧度-->
    <corners android:radius="@dimen/device_item_background_ridus"/>
    <!-- 实心 -->
    <solid android:color="@color/white"/>
    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/line_clolor" />
    <size
        android:height="@dimen/band_select_layout_height"/>
</shape>