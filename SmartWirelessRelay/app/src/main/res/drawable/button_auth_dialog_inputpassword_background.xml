<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:useLevel="false">
    <!--圆角弧度-->
    <corners android:radius="@dimen/ridus2"/>
    <!-- 实心 -->
    <solid android:color="@color/white"/>
    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/room_type_text" />
    <size
        android:width="@dimen/test_response_button_height"
        android:height="@dimen/test_response_button_height"/>
</shape>