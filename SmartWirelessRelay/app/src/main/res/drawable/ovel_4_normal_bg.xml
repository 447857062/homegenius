<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:useLevel="false">
    <!-- 实心 -->
    <solid android:color="@color/white"/>
    <corners
        android:bottomLeftRadius="@dimen/ridus4"
        android:bottomRightRadius="@dimen/ridus4"
        android:topLeftRadius="@dimen/ridus4"
        android:topRightRadius="@dimen/ridus4" />

    <stroke
        android:width="1dp"
        android:color="@color/title_blue_bg" />
    <size
        android:height="30dp"/>
</shape>